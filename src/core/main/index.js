const { Role } = require('./role');
const { User } = require('./user');
const { Person } = require('./person');
const { Level } = require('./level');

module.exports = {
  Role,
  User,
  Person,
  Level,
};
